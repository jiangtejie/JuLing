<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lxjl.juling.module.crm.dal.mysql.statistics.CrmStatisticsPerformanceMapper">

    <select id="selectContractCountPerformance"
            resultType="com.lxjl.juling.module.crm.controller.admin.statistics.vo.performance.CrmStatisticsPerformanceRespVO">
        SELECT
            to_char(order_date, 'YYYYMM') AS time,
            COUNT(1) AS currentMonthCount
        FROM crm_contract
        WHERE deleted = 0
        AND audit_status = ${@com.lxjl.juling.module.crm.enums.common.CrmAuditStatusEnum@APPROVE.status}
        AND owner_user_id in
            <foreach collection="userIds" item="userId" open="(" close=")" separator=",">
                #{userId}
            </foreach>
        AND order_date between #{times[0],javaType=java.time.LocalDateTime} and
            #{times[1],javaType=java.time.LocalDateTime}
        GROUP BY to_char(order_date, 'YYYYMM')
    </select>

    <select id="selectContractPricePerformance"
            resultType="com.lxjl.juling.module.crm.controller.admin.statistics.vo.performance.CrmStatisticsPerformanceRespVO">
        SELECT
            to_char(order_date, 'YYYYMM') AS time,
            COALESCE(SUM(total_price), 0) AS currentMonthCount
        FROM crm_contract
        WHERE deleted = 0
        AND audit_status = ${@com.lxjl.juling.module.crm.enums.common.CrmAuditStatusEnum@APPROVE.status}
        AND owner_user_id in
            <foreach collection="userIds" item="userId" open="(" close=")" separator=",">
                #{userId}
            </foreach>
        AND order_date between #{times[0],javaType=java.time.LocalDateTime} and
            #{times[1],javaType=java.time.LocalDateTime}
        GROUP BY to_char(order_date, 'YYYYMM')
    </select>

    <select id="selectReceivablePricePerformance"
            resultType="com.lxjl.juling.module.crm.controller.admin.statistics.vo.performance.CrmStatisticsPerformanceRespVO">
        SELECT
            to_char(return_time, 'YYYYMM') AS time,
            COALESCE(SUM(price), 0) AS currentMonthCount
        FROM crm_receivable
        WHERE deleted = 0
        AND audit_status = ${@com.lxjl.juling.module.crm.enums.common.CrmAuditStatusEnum@APPROVE.status}
        AND owner_user_id in
            <foreach collection="userIds" item="userId" open="(" close=")" separator=",">
                #{userId}
            </foreach>
        AND return_time between #{times[0],javaType=java.time.LocalDateTime} and
            #{times[1],javaType=java.time.LocalDateTime}
        GROUP BY to_char(return_time, 'YYYYMM')
    </select>

    <select id="selectContractSummary"
            resultType="com.lxjl.juling.module.crm.controller.admin.statistics.vo.performance.CrmStatisticsPerformanceSummaryRespVO">
        SELECT to_char(contract.order_date, 'YYYYMM') AS time,
               COUNT(DISTINCT contract.id) AS contract_count,
               COALESCE(SUM(contract.total_price), 0) AS contract_price,
               COALESCE(SUM(receivable.receivable_price), 0) AS receivable_price,
               COALESCE(SUM(contract.total_price), 0) - COALESCE(SUM(receivable.receivable_price), 0) AS unreceived_price
          FROM crm_contract AS contract
                LEFT JOIN (
                    SELECT contract_id,
                           COALESCE(SUM(price), 0) AS receivable_price
                      FROM crm_receivable
                     WHERE deleted = 0
                       AND audit_status = ${@com.lxjl.juling.module.crm.enums.common.CrmAuditStatusEnum@APPROVE.status}
                     GROUP BY contract_id
                ) AS receivable ON contract.id = receivable.contract_id
         WHERE contract.deleted = 0
           AND contract.audit_status = ${@com.lxjl.juling.module.crm.enums.common.CrmAuditStatusEnum@APPROVE.status}
           AND contract.owner_user_id in
                <foreach collection="userIds" item="userId" open="(" close=")" separator=",">
                    #{userId}
                </foreach>
           AND contract.order_date between #{times[0],javaType=java.time.LocalDateTime} and
                #{times[1],javaType=java.time.LocalDateTime}
         GROUP BY to_char(contract.order_date, 'YYYYMM')
    </select>

</mapper>

