<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <parent>
        <groupId>com.lxjl.boot</groupId>
        <artifactId>juling-module-mall</artifactId>
        <version>${revision}</version>
    </parent>
    <modelVersion>4.0.0</modelVersion>
    <artifactId>juling-module-statistics</artifactId>
    <packaging>jar</packaging>

    <name>${project.artifactId}</name>
    <description>
        statistics 模块，主要实现统计相关功能
        例如：统计商品、会员、交易等功能。
    </description>

    <dependencies>
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-module-promotion</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-module-product</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-module-trade-api</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-module-member</artifactId>
            <version>${revision}</version>
        </dependency>
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-module-pay</artifactId>
            <version>${revision}</version>
        </dependency>

        <!-- 业务组件 -->
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-spring-boot-starter-biz-tenant</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-spring-boot-starter-biz-ip</artifactId>
        </dependency>

        <!-- Web 相关 -->
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-spring-boot-starter-security</artifactId>
        </dependency>

        <!-- DB 相关 -->
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-spring-boot-starter-mybatis</artifactId>
        </dependency>

        <!-- Test 测试相关 -->
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-spring-boot-starter-test</artifactId>
        </dependency>

        <!-- 工具类相关 -->
        <dependency>
            <groupId>com.lxjl.boot</groupId>
            <artifactId>juling-spring-boot-starter-excel</artifactId>
        </dependency>
    </dependencies>

</project>
